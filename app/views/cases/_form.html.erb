<%= form_for(@case) do |f| %>
  <% if @case.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@case.errors.count, "error") %> prohibited this case from being saved:</h2>

      <ul>
      <% @case.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :news_feed %><br />
    <%= f.text_field :news_feed %>
  </div>
  <div class="field">
    <%= f.label :case_date %><br />
    <%= f.date_select :case_date %>
  </div>

    <div class="field">
      <%= f.label :case_img %><br />
      <%= f.file_field :case_img %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
