<%= form_for(@killer) do |f| %>
  <% if @killer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@killer.errors.count, "error") %> prohibited this killer from being saved:</h2>

      <ul>
      <% @killer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :killer_img %><br />
    <%= f.text_field :killer_img %>
  </div>
  <div class="field">
    <%= f.label :statement %><br />
    <%= f.text_area :statement %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
